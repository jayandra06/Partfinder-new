<Page
    x:Class="PartFinder.Views.Pages.WorksheetRelationsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:components="using:PartFinder.Views.Components"
    xmlns:converters="using:PartFinder.Converters">
    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>
    <Grid Padding="20" RowSpacing="12" Background="{StaticResource PageBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <components:PageHeader Title="Link Templates" Subtitle="Map match keys from primary template to lookup template display columns." />

        <Grid Grid.Row="1" ColumnSpacing="14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource CardStyle}">
                <StackPanel Spacing="10">
                    <TextBlock Text="Primary Template" Style="{StaticResource CardTitleStyle}" />
                    <ComboBox
                        Style="{StaticResource StandardTextBoxStyle}"
                        DisplayMemberPath="Name"
                        ItemsSource="{Binding Templates}"
                        SelectedItem="{Binding PrimaryTemplate, Mode=TwoWay}" />
                    <ItemsControl ItemsSource="{Binding PrimaryColumns}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="8" Margin="0,0,0,6" Background="{StaticResource NavItemSelectedBrush}">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding ColumnName}" VerticalAlignment="Center" Style="{StaticResource BodyStyle}" />
                                        <Border Background="{StaticResource AccentPrimaryBrush}" CornerRadius="10" Padding="8,2,8,2">
                                            <TextBlock Text="key" Foreground="{StaticResource TextOnAccentBrush}" FontSize="11" />
                                        </Border>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <FontIcon Grid.Column="1" Glyph="&#xE72A;" FontSize="24" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" />

            <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                <StackPanel Spacing="10">
                    <TextBlock Text="Lookup Template" Style="{StaticResource CardTitleStyle}" />
                    <ComboBox
                        Style="{StaticResource StandardTextBoxStyle}"
                        DisplayMemberPath="Name"
                        ItemsSource="{Binding Templates}"
                        SelectedItem="{Binding LookupTemplate, Mode=TwoWay}" />
                    <ItemsControl ItemsSource="{Binding LookupColumns}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="8" Margin="0,0,0,6" Background="{StaticResource GridRowAltBackgroundBrush}">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <CheckBox
                                            IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                            IsEnabled="{Binding IsSelectable}"
                                            VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding ColumnName}" VerticalAlignment="Center" Style="{StaticResource BodyStyle}" />
                                        <Border Background="{StaticResource SuccessBrush}" CornerRadius="10" Padding="8,2,8,2">
                                            <TextBlock Text="display" Foreground="{StaticResource TextOnAccentBrush}" FontSize="11" />
                                        </Border>
                                        <Border
                                            Visibility="{Binding IsAutoMatchKey, Converter={StaticResource BoolToVisibilityConverter}}"
                                            Background="{StaticResource AccentPrimaryBrush}"
                                            CornerRadius="10"
                                            Padding="8,2,8,2">
                                            <TextBlock Text="key" Foreground="{StaticResource TextOnAccentBrush}" FontSize="11" />
                                        </Border>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="14">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Ellipse Width="10" Height="10" Fill="{StaticResource AccentPrimaryBrush}" />
                <TextBlock VerticalAlignment="Center" Text="Match key (shared column)" Foreground="{StaticResource TextSecondaryBrush}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Ellipse Width="10" Height="10" Fill="{StaticResource SuccessBrush}" />
                <TextBlock VerticalAlignment="Center" Text="Display column (lookup)" Foreground="{StaticResource TextSecondaryBrush}" />
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="10">
            <Button Style="{StaticResource PrimaryButtonStyle}" Content="Save relation" Command="{Binding SaveCommand}" HorizontalAlignment="Right" />
            <ProgressRing Width="18" Height="18" IsActive="{Binding IsLoading}" />
            <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource SuccessBrush}" VerticalAlignment="Center" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" VerticalAlignment="Center" TextWrapping="WrapWholeWords" />
        </StackPanel>
    </Grid>
</Page>
