<UserControl
    x:Class="PartFinder.Views.Components.FavouritesSubPage"
    x:Name="FavouritesSubPageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:PartFinder.Converters"
    Background="{StaticResource PageBackgroundBrush}"
    Loaded="OnLoaded"
    Unloaded="OnUnloaded"
    KeyDown="OnKeyDown">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:InvertedBoolToVisibilityConverter x:Key="InvertedBoolToVisibilityConverter" />

        <!-- Entrance animation -->
        <Storyboard x:Key="EntranceSb">
            <DoubleAnimation
                Storyboard.TargetName="CarouselRoot"
                Storyboard.TargetProperty="Opacity"
                From="0" To="1" Duration="0:0:0.5">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation
                Storyboard.TargetName="RootSlideTransform"
                Storyboard.TargetProperty="Y"
                From="20" To="0" Duration="0:0:0.5">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <!-- Exit animation -->
        <Storyboard x:Key="ExitSb">
            <DoubleAnimation
                Storyboard.TargetName="CarouselRoot"
                Storyboard.TargetProperty="Opacity"
                From="1" To="0" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn" />
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </UserControl.Resources>

    <Grid x:Name="CarouselRoot" Opacity="0">
        <Grid.RenderTransform>
            <TranslateTransform x:Name="RootSlideTransform" Y="20"/>
        </Grid.RenderTransform>

        <Grid.RowDefinitions>
            <RowDefinition Height="0" />
            <RowDefinition Height="*" />
            <RowDefinition Height="80" />
        </Grid.RowDefinitions>

        <!-- ── Empty State ── -->
        <StackPanel
            Grid.Row="1"
            Spacing="24"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}">

            <Border Width="120" Height="120" CornerRadius="60"
                    Background="{StaticResource CardBackgroundBrush}"
                    BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="2"
                    HorizontalAlignment="Center">
                <Border.Shadow>
                    <ThemeShadow/>
                </Border.Shadow>
                <FontIcon Glyph="&#xE735;" FontSize="48" Foreground="{StaticResource AccentPrimaryBrush}" />
            </Border>

            <StackPanel Spacing="12" HorizontalAlignment="Center">
                <TextBlock Text="Your favourites empty" FontSize="28" FontWeight="SemiBold"
                          HorizontalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}" />
                <TextBlock Text="Star your favorite templates to see them here."
                           FontSize="15" Foreground="{StaticResource TextTertiaryBrush}"
                           HorizontalAlignment="Center" TextAlignment="Center"
                           MaxWidth="400" TextWrapping="WrapWholeWords" />
            </StackPanel>
        </StackPanel>

        <!-- ── Carousel canvas (full height, clipped) ── -->
        <Canvas
            x:Name="CarouselCanvas"
            Grid.Row="1"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Visibility="{Binding IsEmpty, Converter={StaticResource InvertedBoolToVisibilityConverter}}">
            <Canvas.Clip>
                <RectangleGeometry x:Name="CarouselClip" />
            </Canvas.Clip>
            <!-- Cards positioned absolutely in code-behind -->
        </Canvas>

        <!-- ── Bottom bar: counter + prev/next buttons ── -->
        <Grid Grid.Row="2"
              Margin="0,0,0,0"
              Visibility="{Binding IsEmpty, Converter={StaticResource InvertedBoolToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Counter label centred -->
            <TextBlock Grid.Column="0" Grid.ColumnSpan="5"
                       Text="{Binding IndexLabel}"
                       FontSize="14"
                       Foreground="{StaticResource TextTertiaryBrush}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontWeight="Medium"
                       Margin="0,0,0,0" />

            <!-- Prev button -->
            <Button x:Name="PrevButton"
                    Grid.Column="1"
                    Width="36" Height="36"
                    Padding="0"
                    Margin="0,0,12,0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    CornerRadius="18"
                    Background="Transparent"
                    BorderThickness="0"
                    IsEnabled="{Binding CanGoPrevious}"
                    Click="OnPreviousClick"
                    AutomationProperties.Name="Previous template">
                <Button.Resources>
                    <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="Transparent" />
                    <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="Transparent" />
                </Button.Resources>
                <FontIcon Glyph="&#xE76B;" FontSize="18" Foreground="White" />
            </Button>

            <!-- Spacer -->
            <Border Grid.Column="2" Width="50" />

            <!-- Next button -->
            <Button x:Name="NextButton"
                    Grid.Column="3"
                    Width="36" Height="36"
                    Padding="0"
                    Margin="12,0,0,0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    CornerRadius="18"
                    Background="Transparent"
                    BorderThickness="0"
                    IsEnabled="{Binding CanGoNext}"
                    Click="OnNextClick"
                    AutomationProperties.Name="Next template">
                <Button.Resources>
                    <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="Transparent" />
                    <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="Transparent" />
                </Button.Resources>
                <FontIcon Glyph="&#xE76C;" FontSize="18" Foreground="White" />
            </Button>
        </Grid>

    </Grid>
</UserControl>
