<Page
    x:Class="PartFinder.Views.Pages.PartsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:components="using:PartFinder.Views.Components">
    <Page.Resources>
        <Style x:Key="ExplorerListItemStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="UseSystemFocusVisuals" Value="False" />
            <Setter Property="CornerRadius" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Grid>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootBorder" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource RowHoverBackgroundBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="SelectionStates">
                                    <VisualState x:Name="Unselected">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="SelectionBar" Storyboard.TargetProperty="Visibility">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="Collapsed" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Selected">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootBorder" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource RowSelectedBackgroundBrush}" />
                                            </ObjectAnimationUsingKeyFrames>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="SelectionBar" Storyboard.TargetProperty="Visibility">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="Visible" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Border
                                x:Name="SelectionBar"
                                Width="2"
                                HorizontalAlignment="Left"
                                Background="{StaticResource RowSelectedBorderBrush}"
                                Visibility="Collapsed" />
                            <Border
                                x:Name="RootBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource BorderDefaultBrush}"
                                BorderThickness="0,0,0,1"
                                Padding="{TemplateBinding Padding}">
                                <ContentPresenter />
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="{StaticResource PageBackgroundBrush}">
        <StackPanel Padding="20" Spacing="12">

            <!-- Header -->
            <Grid ColumnSpacing="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Spacing="2">
                    <TextBlock Text="EXPLORER" FontSize="10" Foreground="{StaticResource AccentPrimaryBrush}" CharacterSpacing="100" FontWeight="SemiBold" />
                    <TextBlock Text="Parts Inventory" FontSize="22" FontWeight="SemiBold" />
                    <TextBlock Text="Browse and explore your parts inventory. Right-click any cell or use row actions to view linked details and relationships." FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                </StackPanel>
                <Button Grid.Column="1" Style="{StaticResource OutlinedButtonStyle}" Content="Refresh" FontSize="12" Command="{Binding RefreshCommand}" VerticalAlignment="Center" />
            </Grid>

            <!-- Template selector -->
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Min(420,*)" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ComboBox
                    Style="{StaticResource StandardTextBoxStyle}"
                    ItemsSource="{Binding Templates}"
                    SelectedItem="{Binding SelectedTemplate, Mode=TwoWay}"
                    DisplayMemberPath="Name"
                    PlaceholderText="Select template"
                    FontSize="12" />
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Left">
                    <Button Style="{StaticResource GhostButtonStyle}" Content="Load More" FontSize="11" Visibility="Collapsed" Command="{Binding LoadMoreCommand}" />
                </StackPanel>
            </Grid>

            <!-- Filters bar -->
            <Border Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1" CornerRadius="4" Padding="14,10" Translation="0,0,32">
                            <Border.Shadow><ThemeShadow/></Border.Shadow>
                            <StackPanel Spacing="6">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Style="{StaticResource OutlinedButtonStyle}" Content="Clear all filters" FontSize="11" Command="{Binding ClearAllFiltersCommand}" />
                        <TextBlock VerticalAlignment="Center" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}">
                            <Run Text="Showing " />
                            <Run Text="{Binding FilteredRecordsCount}" />
                            <Run Text=" rows" />
                        </TextBlock>
                        <TextBlock VerticalAlignment="Center" FontSize="11" Foreground="{StaticResource TextTertiaryBrush}">
                            <Run Text="of " />
                            <Run Text="{Binding TotalRecordsCount}" />
                            <Run Text=" loaded" />
                        </TextBlock>
                    </StackPanel>
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                        <StackPanel x:Name="ColumnFiltersPanel" Orientation="Horizontal" Spacing="0" />
                    </ScrollViewer>
                </StackPanel>
            </Border>

            <!-- Data table -->
            <Border Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1" CornerRadius="4" Padding="0" Translation="0,0,32">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" MinHeight="420">
                    <StackPanel>
                        <ListView
                            x:Name="PartsListView"
                            ItemContainerStyle="{StaticResource ExplorerListItemStyle}"
                            ItemsSource="{Binding FilteredRecords}"
                            SelectionChanged="OnPartsSelectionChanged"
                            ContainerContentChanging="OnContainerContentChanging">
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsStackPanel />
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid />
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                        <StackPanel Margin="0,8,0,0" Orientation="Horizontal" Spacing="8" Visibility="Collapsed">
                            <ProgressRing Width="14" Height="14" IsActive="{Binding IsLoading}" />
                            <TextBlock Foreground="{ThemeResource AppSubtleTextBrush}" FontSize="11" Text="Tip: use right-click on a cell or the ... button at row end for actions." />
                        </StackPanel>

                        <Border Margin="0,10,0,0" Padding="10" BorderThickness="1" BorderBrush="{ThemeResource AppBorderBrush}" Visibility="Collapsed">
                            <StackPanel Spacing="6">
                                <TextBlock Text="Actions" Style="{ThemeResource AppSectionTextStyle}" FontSize="12" />
                                <StackPanel x:Name="RowActionsPanel" Orientation="Horizontal" Spacing="6" />
                                <TextBlock
                                    x:Name="RowActionsHintText"
                                    Foreground="{ThemeResource AppSubtleTextBrush}"
                                    FontSize="11"
                                    Text="Select a row to see available actions." />
                                <Button
                                    x:Name="ClearSelectedRowButton"
                                    HorizontalAlignment="Left"
                                    Content="Clear selected row"
                                    FontSize="11"
                                    Visibility="Collapsed"
                                    Click="OnClearSelectedRowClick" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </StackPanel>

        <!-- Action overlay -->
        <Border
            x:Name="ActionResultsOverlay"
            Background="#66000000"
            Visibility="Collapsed"
            Tapped="OnActionOverlayTapped" />

        <!-- Action results slide panel -->
        <Border
            x:Name="ActionResultsPanel"
            Width="320"
            HorizontalAlignment="Right"
            VerticalAlignment="Stretch"
            Background="{StaticResource ElevatedCardBackgroundBrush}"
            BorderBrush="{StaticResource BorderDefaultBrush}"
            BorderThickness="1,0,0,0"
            Visibility="Collapsed">
            <Border.RenderTransform>
                <TranslateTransform x:Name="ActionResultsTransform" X="320" />
            </Border.RenderTransform>
            <Grid RowSpacing="8" Padding="14">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Spacing="4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="ActionTitleText" Text="Details" FontSize="14" FontWeight="SemiBold" />
                        <Button Grid.Column="1" Style="{StaticResource GhostButtonStyle}" Content="âœ•" FontSize="12" Click="OnCloseActionPanelClick" />
                    </Grid>
                    <TextBlock x:Name="ActionCountText" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" />
                </StackPanel>

                <ListView x:Name="ActionRowsListView" Grid.Row="1">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource BorderDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,0,0,6" Translation="0,0,32">
                            <Border.Shadow><ThemeShadow/></Border.Shadow>
                            <StackPanel Spacing="3">
                                    <TextBlock Text="{Binding Title}" FontSize="12" FontWeight="SemiBold" />
                                    <ItemsControl ItemsSource="{Binding Lines}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" />
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>



